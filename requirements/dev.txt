# Test & lint tooling. Deliberately NOT installed into the published images —
# these were previously in requirements.txt and shipped to GHCR (~80 MB).
#
#   pip install -r requirements.txt -r requirements/dev.txt

-r core.txt
-r data.txt
-r extras.txt

pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
pytest-cov>=6.0.0,<7.0.0
ruff>=0.8.0,<1.0.0
mypy>=1.13.0,<2.0.0
