# Vector DB, LLM routing, image I/O and report exports.
# Medium-sized, moderate churn -> third parallel layer.

# --- Vector DB ---
qdrant-client>=1.9.0,<2.0.0

# --- Multi-LLM provider routing (OpenAI / Anthropic / Groq / Ollama / custom) ---
# Highest-churn dependency in the project: the <2.0.0 range spans hundreds of
# releases with frequent provider-routing changes. Worth pinning tighter once a
# lockfile exists.
litellm>=1.40.0,<2.0.0

# --- Image I/O ---
# Pillow's manylinux wheels bundle libjpeg/zlib, so the runtime image needs no
# libjpeg-dev / libpng-dev.
Pillow>=10.4.0,<12.0.0

# --- Report exports (EDA download as PDF / DOCX) ---
reportlab>=4.2.0,<5.0.0
python-docx>=1.1.0,<2.0.0
